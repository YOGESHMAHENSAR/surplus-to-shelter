{
  "name": "surplus-to-shelter",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "vision": "cd vision-service && python -m uvicorn main:app --port 8000",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": { "concurrently": "^8.2.2" }
}
